# Canonical dependency declarations and optional groups live in backend/pyproject.toml.
# The constraints lock reproduces the tested Python 3.12 app/dev environment.
-c requirements-lock.txt
-e ./backend[dev]
